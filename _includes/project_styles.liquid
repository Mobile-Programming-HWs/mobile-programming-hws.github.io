<style>
  .project-hub-lead {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.95fr);
    gap: clamp(1.25rem, 4vw, 3rem);
    align-items: center;
    margin: 0.25rem 0 2.5rem;
    padding: clamp(0.5rem, 2vw, 1.25rem) 0;
  }

  h1.post-title {
    max-width: 100%;
    margin-bottom: 1rem;
    white-space: normal;
    overflow-wrap: anywhere;
  }

  h1.post-title::after {
    content: '';
    display: block;
    width: 4.5rem;
    height: 0.18rem;
    margin-top: 0.8rem;
    border-radius: 999px;
    background: linear-gradient(90deg, #1c7ed6, #2b8a3e 42%, #e67700);
  }

  .project-hub-copy {
    --project-accent: #1c7ed6;
    max-width: 42rem;
  }

  .project-hub-kicker {
    display: inline-flex;
    align-items: center;
    margin-bottom: 0.85rem;
    padding: 0.32rem 0.65rem;
    border: 1px solid color-mix(in srgb, var(--project-accent) 45%, transparent);
    border-radius: 8px;
    color: var(--project-accent);
    background: color-mix(in srgb, var(--project-accent) 10%, transparent);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .project-hub-intro {
    max-width: 39rem;
    font-size: clamp(1rem, 1.2vw, 1.12rem);
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .project-action-row {
    --project-accent: #1c7ed6;
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.25rem;
  }

  .project-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.65rem;
    padding: 0.62rem 0.95rem;
    border: 1px solid var(--global-divider-color, #d8d8d8);
    border-radius: 8px;
    background: color-mix(in srgb, var(--global-bg-color) 82%, var(--project-accent));
    color: var(--project-accent);
    font-weight: 500;
    text-decoration: none;
    transition:
      transform 160ms ease,
      border-color 160ms ease,
      background 160ms ease;
  }

  .project-action:hover,
  .project-link-row a:hover {
    border-color: color-mix(in srgb, var(--project-accent) 72%, var(--global-divider-color));
    color: var(--project-accent);
    text-decoration: none;
    transform: translateY(-1px);
  }

  .project-action-primary {
    border-color: var(--project-accent);
    background: var(--project-accent);
    color: var(--global-hover-text-color);
  }

  .project-action-primary:hover {
    color: var(--global-hover-text-color);
    background: color-mix(in srgb, var(--project-accent) 82%, #111827);
  }

  .project-hub-stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
    margin: 1.25rem 0 0;
  }

  .project-hub-stat,
  .project-hub-card,
  .project-page-card,
  .repo-link-card {
    border: 1px solid var(--global-divider-color, #d8d8d8);
    border-radius: 8px;
    background: color-mix(in srgb, var(--global-bg-color) 92%, var(--project-accent, var(--global-theme-color)) 8%);
    box-shadow: 0 0.45rem 1.25rem rgba(0, 0, 0, 0.08);
  }

  .project-hub-stat {
    padding: 0.85rem 0.95rem;
  }

  .project-hub-stat strong {
    display: block;
    font-size: 1.45rem;
    line-height: 1.2;
  }

  .project-hub-preview {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.75rem;
    align-self: stretch;
  }

  .project-hub-preview img,
  .project-page-image img {
    width: 100%;
    object-fit: cover;
    object-position: top;
    background: var(--global-bg-color, #ffffff);
  }

  .project-hub-preview img {
    aspect-ratio: 5 / 4;
    border-radius: 8px;
    border: 1px solid var(--global-divider-color, #d8d8d8);
    box-shadow: 0 0.35rem 1rem rgba(0, 0, 0, 0.18);
    transition:
      transform 180ms ease,
      box-shadow 180ms ease;
  }

  .project-hub-grid,
  .repo-link-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 220px), 1fr));
    gap: 1rem;
    margin: 1rem 0 2rem;
  }

  .project-hub-card,
  .repo-link-card {
    border-left: 0.3rem solid var(--project-accent, var(--global-theme-color, #2698ba));
    padding: 1.05rem;
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      box-shadow 180ms ease;
  }

  .project-hub-card {
    min-height: 13.5rem;
  }

  .project-hub-card:hover,
  .project-page-card:hover,
  .repo-link-card:hover {
    border-color: color-mix(in srgb, var(--project-accent, var(--global-theme-color)) 58%, var(--global-divider-color));
    box-shadow: 0 0.8rem 1.8rem rgba(0, 0, 0, 0.14);
    transform: translateY(-2px);
  }

  .project-hub-preview img:hover {
    box-shadow: 0 0.8rem 1.8rem rgba(0, 0, 0, 0.18);
    transform: translateY(-2px);
  }

  .project-hub-card h2,
  .repo-link-card h2 {
    font-size: 1.12rem;
    margin: 0.25rem 0 0.35rem;
  }

  .project-hub-card a,
  .project-page-card a,
  .repo-link-card a {
    color: var(--project-accent, var(--global-theme-color));
  }

  .project-hub-card p,
  .repo-link-card p {
    line-height: 1.55;
    margin-bottom: 0.75rem;
  }

  .project-hub-card ul,
  .project-page-card ul {
    padding-left: 1.1rem;
    margin-bottom: 0;
  }

  .project-hub-card li,
  .project-page-card li {
    margin-bottom: 0.25rem;
  }

  .project-hub-type,
  .project-page-type {
    display: inline-block;
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--project-accent, var(--global-theme-color));
  }

  .project-page-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 260px), 1fr));
    gap: 1.25rem;
    margin: 1rem 0 2.25rem;
  }

  .project-page-grid-narrow {
    max-width: 44rem;
  }

  .project-page-card {
    overflow: hidden;
    border-top: 0.3rem solid var(--project-accent, var(--global-theme-color, #2698ba));
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      box-shadow 180ms ease;
  }

  .project-page-image img {
    aspect-ratio: 16 / 10;
    border-bottom: 1px solid var(--global-divider-color, #d8d8d8);
  }

  .project-page-body {
    padding: 1.1rem;
  }

  .project-page-card h2 {
    font-size: 1.45rem;
    margin: 0.25rem 0 0.45rem;
  }

  .project-link-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1rem;
  }

  .project-link-row a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.35rem;
    padding: 0.45rem 0.72rem;
    border: 1px solid color-mix(in srgb, var(--project-accent, var(--global-theme-color)) 34%, transparent);
    border-radius: 8px;
    background: color-mix(in srgb, var(--project-accent, var(--global-theme-color)) 8%, transparent);
    font-weight: 500;
    transition:
      transform 160ms ease,
      border-color 160ms ease,
      background 160ms ease;
  }

  .accent-sky {
    --project-accent: #1c7ed6;
  }

  .accent-green {
    --project-accent: #2b8a3e;
  }

  .accent-red {
    --project-accent: #c92a2a;
  }

  .accent-gold {
    --project-accent: #e67700;
  }

  @media (max-width: 767px) {
    .project-hub-lead {
      grid-template-columns: 1fr;
      margin-bottom: 2rem;
    }

    .project-hub-stats {
      grid-template-columns: 1fr;
    }

    .project-page-grid,
    .project-hub-grid,
    .repo-link-grid {
      gap: 0.85rem;
    }
  }

  @media (max-width: 575px) {
    h1.post-title {
      font-size: 2rem;
      line-height: 1.2;
    }

    .project-hub-intro {
      font-size: 1rem;
    }

    .project-action,
    .project-link-row a {
      width: 100%;
    }

    .project-hub-preview {
      gap: 0.55rem;
    }

    .project-hub-preview img {
      aspect-ratio: 4 / 3;
    }

    .project-hub-card,
    .project-page-body,
    .repo-link-card {
      padding: 0.95rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project-action,
    .project-hub-card,
    .project-hub-preview img,
    .project-page-card,
    .repo-link-card,
    .project-link-row a {
      transition: none;
    }

    .project-action:hover,
    .project-hub-card:hover,
    .project-hub-preview img:hover,
    .project-page-card:hover,
    .repo-link-card:hover,
    .project-link-row a:hover {
      transform: none;
    }
  }
</style>
